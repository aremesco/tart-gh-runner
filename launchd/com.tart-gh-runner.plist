<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.tart-gh-runner</string>
  <key>ProgramArguments</key>
  <array><string>/usr/local/bin/tart-gh-runner</string></array>
  <!-- KeepAlive IS the loop. The script takes exactly one job and exits, so
       relaunching it is what gives the next job a clean VM. -->
  <key>KeepAlive</key><true/>
  <!-- Bounds a crash loop: a missing config, absent tart or unbuilt image
       would otherwise respawn as fast as launchd can fork. -->
  <key>ThrottleInterval</key><integer>30</integer>
  <key>SoftResourceLimits</key>
  <dict><key>NumberOfProcesses</key><integer>1024</integer></dict>
  <key>EnvironmentVariables</key>
  <dict><key>PATH</key><string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string></dict>
  <key>StandardOutPath</key><string>/tmp/tart-gh-runner.log</string>
  <key>StandardErrorPath</key><string>/tmp/tart-gh-runner.log</string>
</dict>
</plist>
